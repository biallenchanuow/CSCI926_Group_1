from selenium.webdriver.common.by import By
from src.testproject.classes import DriverStepSettings, StepSettings
from src.testproject.decorator import report_assertion_errors
from src.testproject.enums import SleepTimingType
from src.testproject.sdk.drivers import webdriver
import pytest


"""
This pytest test was automatically generated by TestProject
    Project: Jupyter Notebook Functional Test
    Package: TestProject.Generated.Tests.JupyterNotebookFunctionalTest
    Test: Batch Operations - Edit, View, Shutdown, Delete
    Generated by: Jianpeng Chen (jc107@uowmail.edu.au)
    Generated on 05/24/2021, 04:04:22
"""


@pytest.fixture()
def driver():
    driver = webdriver.Chrome(token="fk3yOvlRsLLpCaaggFfMO8vEplPSx8qsdrnNiqcDOgM",
                              project_name="Jupyter Notebook Functional Test",
                              job_name="Batch Operations - Edit, View, Shutdown, Delete")
    step_settings = StepSettings(timeout=15000,
                                 sleep_time=500,
                                 sleep_timing_type=SleepTimingType.Before)
    with DriverStepSettings(driver, step_settings):
        yield driver
    driver.quit()


@report_assertion_errors
def test_main(driver):
    # Test Parameters
    # Auto generated application URL parameter
    ApplicationURL = "http://localhost:8888/tree"

    # 1. Navigate to '{ApplicationURL}'
    # Navigates the specified URL (Auto-generated)
    driver.get(f'{ApplicationURL}')

    # 2. Click 'functional test folder'
    functional_test_folder = driver.find_element(By.XPATH,
                                                 "//span[. = 'functional test folder']")
    functional_test_folder.click()

    # 3. Click 'test batch operations'
    test_batch_operations = driver.find_element(By.XPATH,
                                                "//span[. = 'test batch operations']")
    test_batch_operations.click()

    # 4. Click 'checkbox
    #
    #          ...'
    checkbox_ = driver.find_element(By.CSS_SELECTOR,
                                    "#tree-selector-btn")
    checkbox_.click()

    # 5. Click 'Folders'
    folders = driver.find_element(By.CSS_SELECTOR,
                                  "#select-folders")
    folders.click()

    # 6. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 7. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 8. Click 'I3'
    i3 = driver.find_element(By.XPATH,
                             "//button[8]/i")
    i3.click()

    # 9. Click 'Delete'
    delete = driver.find_element(By.XPATH,
                                 "//button[. = 'Delete']")
    delete.click()

    # 10. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 11. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 12. Click 'Duplicate'
    duplicate = driver.find_element(By.XPATH,
                                    "//button[. = 'Duplicate ']")
    duplicate.click()

    # 13. Click 'Duplicate1'
    duplicate1 = driver.find_element(By.XPATH,
                                     "//button[. = 'Duplicate']")
    duplicate1.click()

    # 14. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 15. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 16. Click 'Click here to rename, delete, etc.4'
    click_here_to_rename_delete_etc_4 = driver.find_element(By.XPATH,
                                                            "//div[6]//input")
    click_here_to_rename_delete_etc_4.click()

    # 17. Click 'Click here to rename, delete, etc.5'
    click_here_to_rename_delete_etc_5 = driver.find_element(By.XPATH,
                                                            "//div[7]//input")
    click_here_to_rename_delete_etc_5.click()

    # 18. Click 'Shutdown'
    shutdown = driver.find_element(By.XPATH,
                                   "//button[5][. = 'Shutdown']")
    shutdown.click()

    # 19. Click 'Click here to rename, delete, etc.4'
    click_here_to_rename_delete_etc_4 = driver.find_element(By.XPATH,
                                                            "//div[6]//input")
    click_here_to_rename_delete_etc_4.click()

    # 20. Click 'Click here to rename, delete, etc.5'
    click_here_to_rename_delete_etc_5 = driver.find_element(By.XPATH,
                                                            "//div[7]//input")
    click_here_to_rename_delete_etc_5.click()

    # 21. Click 'Move'
    move = driver.find_element(By.XPATH,
                               "//button[. = 'Move']")
    move.click()

    # 22. Click 'INPUT3'
    input3 = driver.find_element(By.XPATH,
                                 "//div[4]/div/div//input")
    input3.click()

    # 23. Type '/functional test folder/test batch operations/batch_1' in 'INPUT3'
    input3 = driver.find_element(By.XPATH,
                                 "//div[4]/div/div//input")
    input3.send_keys("/functional test folder/test batch operations/batch_1")

    # 24. Click 'Move1'
    move1 = driver.find_element(By.XPATH,
                                "//button[2][. = 'Move']")
    move1.click()

    # 25. Click 'batch_1'
    batch_1 = driver.find_element(By.XPATH,
                                  "//span[. = 'batch_1']")
    batch_1.click()

    # 26. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 27. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 28. Click 'View'
    view = driver.find_element(By.XPATH,
                               "//button[. = 'View']")
    view.click()

    # 29. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 30. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 31. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 32. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 33. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 34. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 35. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 36. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 37. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 38. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 39. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 40. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 41. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 42. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 43. Click 'Edit'
    edit = driver.find_element(By.XPATH,
                               "//button[. = 'Edit']")
    edit.click()

    # 44. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 45. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 46. Click 'batch_11'
    batch_11 = driver.find_element(By.XPATH,
                                   "//span[. = 'batch_1']")
    batch_11.click()

    # 47. Click 'Click here to rename, delete, etc.6'
    click_here_to_rename_delete_etc_6 = driver.find_element(By.XPATH,
                                                            "//div[3]//input")
    click_here_to_rename_delete_etc_6.click()

    # 48. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 49. Click ''
    _ = driver.find_element(By.XPATH,
                            "//button[. = '\n                    \n                  ']")
    _.click()

    # 50. Click 'Delete'
    delete = driver.find_element(By.XPATH,
                                 "//button[. = 'Delete']")
    delete.click()

    # 51. Click 'test batch operations1'
    test_batch_operations1 = driver.find_element(By.XPATH,
                                                 "//a[. = 'test batch operations']")
    test_batch_operations1.click()

    # 52. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 53. Click 'Click here to rename, delete, etc.3'
    click_here_to_rename_delete_etc_3 = driver.find_element(By.XPATH,
                                                            "//div[5]//input")
    click_here_to_rename_delete_etc_3.click()

    # 54. Click 'Duplicate'
    duplicate = driver.find_element(By.XPATH,
                                    "//button[. = 'Duplicate ']")
    duplicate.click()

    # 55. Click 'Duplicate1'
    duplicate1 = driver.find_element(By.XPATH,
                                     "//button[. = 'Duplicate']")
    duplicate1.click()

    # 56. Click 'Click here to rename, delete, etc.2'
    click_here_to_rename_delete_etc_2 = driver.find_element(By.XPATH,
                                                            "//div[4]//input")
    click_here_to_rename_delete_etc_2.click()

    # 57. Click 'Click here to rename, delete, etc.5'
    click_here_to_rename_delete_etc_5 = driver.find_element(By.XPATH,
                                                            "//div[7]//input")
    click_here_to_rename_delete_etc_5.click()

    # 58. Click 'View'
    view = driver.find_element(By.XPATH,
                               "//button[. = 'View']")
    view.click()

    # 59. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    # 60. Switch to window '2'
    driver.switch_to.window(driver.window_handles[2])

    # 61. Switch to window '1'
    driver.switch_to.window(driver.window_handles[1])

    #